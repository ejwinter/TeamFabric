# Labels Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a labels system to Fabric — key=value metadata on all backlog entities and requests, defined in the team constitution, with AI-assisted suggestion and deep rollup in Child Summary sections.

**Architecture:** Labels are stored as a single `Labels:` property line on each entity. The team defines valid keys and values (with descriptions) in their CLAUDE.md constitution. `/rollup-backlog` aggregates all descendant labels with occurrence counts into the Child Summary section. Behavioral rules in `fabric-backlog.md` and `fabric-triage.md` govern suggestion and validation.

**Tech Stack:** Markdown entity files, markdown behavioral rule files, slash command definition files.

---

## File Map

| File | Change |
| ---- | ------ |
| `Fabric/backlog/template-epic.md` | Add `Labels:` property |
| `Fabric/backlog/template-feature.md` | Add `Labels:` property |
| `Fabric/backlog/template-workitem.md` | Add `Labels:` property |
| `Fabric/backlog/template-task.md` | Add `Labels:` property |
| `Fabric/template/fabric-backlog.md` | Add Labels section, update entity property lists, add behavioral rules |
| `Fabric/template/fabric-triage.md` | Add Labels support for requests |
| `Fabric/.claude/commands/rollup-backlog.md` | Add label aggregation to scan and write steps |
| `Example/.claude/fabric-backlog.md` | Sync copy of `Fabric/template/fabric-backlog.md` |
| `Example/.claude/fabric-triage.md` | Sync copy of `Fabric/template/fabric-triage.md` |
| `Example/CLAUDE.md` | Add example label schema under `### Labels` in How We Work |
| `Example/requests/workflow/default/request-template.md` | Add `Labels:` property |
| `Example/backlog/epics/ehr-pipeline-modernization/epic.md` | Add example labels |
| `Example/backlog/epics/ehr-pipeline-modernization/features/fhir-r4-support/feature.md` | Add example labels |

---

### Task 1: Add Labels property to backlog entity templates

**Files:**
- Modify: `Fabric/backlog/template-epic.md`
- Modify: `Fabric/backlog/template-feature.md`
- Modify: `Fabric/backlog/template-workitem.md`
- Modify: `Fabric/backlog/template-task.md`

- [ ] **Step 1: Add Labels to template-epic.md**

The property goes after `Area:` (the last property). Replace:

```markdown
- Area: [optional area path representing a product or service line]
```

With:

```markdown
- Area: [optional area path representing a product or service line]
- Labels: [optional, comma-separated key=value pairs matching the team's label schema, e.g. service-type=data-extraction, security-sensitive=true]
```

- [ ] **Step 2: Add Labels to template-feature.md**

Same change — after `Area:`:

```markdown
- Area: [optional area path representing a product or service line]
- Labels: [optional, comma-separated key=value pairs matching the team's label schema, e.g. service-type=data-extraction, security-sensitive=true]
```

- [ ] **Step 3: Add Labels to template-workitem.md**

After `Assigned to:` (the last property):

```markdown
- Assigned to: [optional, the name or email of the person this is assigned to, it is probably a team member]
- Labels: [optional, comma-separated key=value pairs matching the team's label schema, e.g. service-type=data-extraction, security-sensitive=true]
```

- [ ] **Step 4: Add Labels to template-task.md**

After `Remaining Hours:` (the last property):

```markdown
- Remaining Hours: [the number of hours remaining on the task]
- Labels: [optional, comma-separated key=value pairs matching the team's label schema, e.g. service-type=data-extraction, security-sensitive=true]
```

- [ ] **Step 5: Verify the four template files**

Read each of the four files and confirm `Labels:` appears as the last property in the Properties section. Confirm no other properties were disturbed.

- [ ] **Step 6: Commit**

```bash
git add Fabric/backlog/template-epic.md Fabric/backlog/template-feature.md Fabric/backlog/template-workitem.md Fabric/backlog/template-task.md
git commit -m "feat: add Labels property to backlog entity templates"
```

---

### Task 2: Update fabric-backlog.md with Labels documentation

**Files:**
- Modify: `Fabric/template/fabric-backlog.md`

The file has four entity descriptions (Epic, Feature, Work Item, Task), a Behavioral Rules section, and a Commands table. Changes:
1. Add `Labels:` to each entity's Properties list
2. Add a `## Labels` section documenting schema format, storage, suggestion behavior, and rollup
3. Add two behavioral rules for label validation and suggestion

- [ ] **Step 1: Add Labels to Epic properties list**

In the Epic properties block, after `Area: [optional area path representing a product or service line]`, add:

```markdown
- Labels: [optional, comma-separated key=value pairs e.g. service-type=data-extraction]
```

- [ ] **Step 2: Add Labels to Feature properties list**

Same addition after `Area:` in the Feature block.

- [ ] **Step 3: Add Labels to Work Item properties list**

After `Assigned to: [optional]` in the Work Item block, add:

```markdown
- Labels: [optional, comma-separated key=value pairs e.g. service-type=data-extraction]
```

- [ ] **Step 4: Add Labels to Task properties list**

After `Remaining Hours: [optional]` in the Task block, add:

```markdown
- Labels: [optional, comma-separated key=value pairs e.g. service-type=data-extraction]
```

- [ ] **Step 5: Add Labels section**

Insert a new `## Labels` section after the `## Iterations` section and before `## Behavioral Rules`. The section has four subsections — write them verbatim:

**Section heading and intro:**
```
## Labels

Labels are key=value metadata applied to backlog entities. They let teams classify and filter work by custom dimensions (e.g. service type, domain, priority tier). Teams define their label schema in the `### Labels` subsection of "How We Work" in CLAUDE.md.
```

**Schema Format subsection** (the indented list is a schema example in a fenced code block — use triple backticks with `markdown` hint):
```
### Schema Format

Each key and each enumerated value has a description to guide consistent usage. Boolean keys (value type `boolean`) act as tags — only `true` or `false` are valid values.

[fenced code block, language: markdown]
### Labels

- **service-type** — The type of service this work relates to
  - `data-extraction` — Work that pulls data from source systems
  - `reporting` — Work that produces dashboards, reports, or metric outputs

- **security-sensitive** — boolean — Set to true when this work touches sensitive data
[end fenced code block]
```

**Storage subsection** (the property line is in a plain fenced code block):
```
### Storage

Labels are stored on an entity as a single property line:

[fenced code block, no language hint]
- Labels: service-type=data-extraction, security-sensitive=true
[end fenced code block]

The line is omitted when no labels are applied — do not leave it blank or as a placeholder.
```

**AI-Assisted Suggestion subsection:**
```
### AI-Assisted Suggestion

After any operation that writes or updates a description or acceptance criteria section (create, refine, ingest), read those sections and cross-reference against the label schema key and value descriptions. Offer specific suggestions proactively:

> "Based on the description, would you like me to add `service-type=data-extraction`?"

One suggestion per label key. Only suggest when confident from the content. The user confirms or declines each suggestion individually.
```

**Rollup subsection:**
```
### Rollup

`/rollup-backlog` aggregates labels from all descendants (not just direct children) and writes a `Labels (rolled up)` line to the Child Summary section. See the `/rollup-backlog` command definition for aggregation details.
```

- [ ] **Step 6: Add behavioral rules**

In the `## Behavioral Rules` section, add these two rules after the existing rules:

```markdown
- When writing a label value, validate it against the team's label schema in CLAUDE.md. If the value is not listed, flag it and suggest the nearest valid option before writing. If no schema is defined, accept any key=value pair.
- After writing or updating a description or acceptance criteria, cross-reference the content against the label schema descriptions and proactively offer label suggestions. One suggestion per key, only when confident.
```

- [ ] **Step 7: Verify fabric-backlog.md**

Read the file and confirm: Labels appears in all four entity property lists, the `## Labels` section is present and well-formed between Iterations and Behavioral Rules, and the two new behavioral rules are present.

- [ ] **Step 8: Commit**

```bash
git add Fabric/template/fabric-backlog.md
git commit -m "feat: document Labels in fabric-backlog module"
```

---

### Task 3: Update fabric-triage.md with Labels support

**Files:**
- Modify: `Fabric/template/fabric-triage.md`

Requests also support labels. The change is a short behavioral rule — requests don't have children so no rollup is involved.

- [ ] **Step 1: Add Labels behavioral rule to fabric-triage.md**

In the `## Behavioral Rules` section, add:

```markdown
- Request entities support a `Labels:` property in their Properties section, using the same key=value format as backlog entities. The team's label schema in CLAUDE.md applies. After ingesting or updating a request description, offer label suggestions the same way as for backlog entities.
```

- [ ] **Step 2: Verify fabric-triage.md**

Read the file and confirm the Labels rule is present in Behavioral Rules.

- [ ] **Step 3: Commit**

```bash
git add Fabric/template/fabric-triage.md
git commit -m "feat: add Labels support to triage requests"
```

---

### Task 4: Update rollup-backlog command with label aggregation

**Files:**
- Modify: `Fabric/.claude/commands/rollup-backlog.md`

The command needs two additions: (1) collect Labels from all descendants in the scan step, and (2) write the `Labels (rolled up)` line in the write step.

- [ ] **Step 1: Update scan step (step 2)**

The current step 2 reads entity headers and extracts name, state, and level. Update the step to also extract labels:

Replace:
```markdown
2. **Scan children.**
   - List all direct child directories of the target entity.
   - For each child, read the entity file header (everything before the first `##` section) to extract: name (from `# ` title), state, and level.
   - For features: count work item subdirectories and sum estimated hours from task files (if present) for the sizing signal.
   - For epics: count feature subdirectories and note their states.
```

With:
```markdown
2. **Scan children.**
   - List all direct child directories of the target entity.
   - For each child, read the entity file header (everything before the first `##` section) to extract: name (from `# ` title), state, and level.
   - For features: count work item subdirectories and sum estimated hours from task files (if present) for the sizing signal.
   - For epics: count feature subdirectories and note their states.
   - **Label collection (always deep):** Regardless of whether `--deep` is set, traverse *all* descendants (not just direct children) and collect every `Labels:` property value found. Parse each `key=value` pair and aggregate by key with occurrence counts. For boolean keys, count only `true` occurrences.
```

- [ ] **Step 2: Update write step (step 6)**

The current step 6 replaces the Child Summary section. Add the Labels line to the example output format. Replace:

```markdown
6. **Write on confirmation.**
   - Replace the existing `## Child Summary` section or insert it after the last existing section (before `## Context Log` if present).
   - Update the `Last updated` date to today.
```

With:
```markdown
6. **Write on confirmation.**
   - Replace the existing `## Child Summary` section or insert it after the last existing section (before `## Context Log` if present).
   - Update the `Last updated` date to today.
   - If any descendants have labels, include a `Labels (rolled up)` line in the Child Summary using this format:
     ```
     - Labels (rolled up): service-type: data-extraction (5), reporting (2); security-sensitive: true (3)
     ```
     Keys are separated by semicolons. Values within a key are comma-separated with counts in parentheses. Boolean keys show only the `true` count. Omit the line entirely if no descendants have labels.
```

- [ ] **Step 3: Verify rollup-backlog.md**

Read the file and confirm step 2 includes the label collection paragraph and step 6 includes the `Labels (rolled up)` format with the example.

- [ ] **Step 4: Commit**

```bash
git add Fabric/.claude/commands/rollup-backlog.md
git commit -m "feat: add label aggregation to rollup-backlog command"
```

---

### Task 5: Update Example/CLAUDE.md with example label schema

**Files:**
- Modify: `Example/CLAUDE.md`

The Riverdale Data Engineering team gets a representative label schema under `## How We Work`.

- [ ] **Step 1: Add Labels section to Example/CLAUDE.md**

In the `## How We Work` section, after the existing content, add:

```markdown
### Labels

- **service-type** — The type of service this work relates to
  - `data-extraction` — Work that pulls or transforms data from source systems (e.g. EHR feeds, ADT events)
  - `reporting` — Work that produces dashboards, reports, or operational metrics
  - `api` — Work that exposes or consumes internal or external APIs
  - `infrastructure` — Work related to pipelines, platform reliability, or DevOps

- **data-domain** — The clinical or operational data domain this work operates in
  - `clinical` — Patient care data (FHIR, ADT, clinical events)
  - `operational` — Non-clinical business data (department KPIs, census, access)
  - `platform` — Cross-domain infrastructure with no single domain owner

- **security-sensitive** — boolean — Set to true when this work involves PHI, PII, or requires IRB/compliance review
```

- [ ] **Step 2: Verify Example/CLAUDE.md**

Read the file and confirm the `### Labels` subsection is present under `## How We Work` with three keys and correct nested value format.

- [ ] **Step 3: Commit**

```bash
git add Example/CLAUDE.md
git commit -m "feat: add example label schema to Riverdale constitution"
```

---

### Task 6: Add Labels to request template and sync module files

**Files:**
- Modify: `Example/requests/workflow/default/request-template.md`
- Modify: `Example/.claude/fabric-backlog.md`
- Modify: `Example/.claude/fabric-triage.md`

- [ ] **Step 1: Add Labels to request-template.md**

In the `## Properties` section, after `External:`, add:

```markdown
Labels: [optional, comma-separated key=value pairs matching the team's label schema, e.g. service-type=data-extraction]
```

- [ ] **Step 2: Sync Example/.claude/fabric-backlog.md**

Replace the entire contents of `Example/.claude/fabric-backlog.md` with the updated contents of `Fabric/template/fabric-backlog.md`. These files must stay identical.

- [ ] **Step 3: Sync Example/.claude/fabric-triage.md**

Replace the entire contents of `Example/.claude/fabric-triage.md` with the updated contents of `Fabric/template/fabric-triage.md`. These files must stay identical.

- [ ] **Step 4: Verify the three files**

- Read `Example/requests/workflow/default/request-template.md` — confirm `Labels:` appears in Properties after `External:`
- Confirm `Example/.claude/fabric-backlog.md` matches `Fabric/template/fabric-backlog.md` (same Labels section, same behavioral rules)
- Confirm `Example/.claude/fabric-triage.md` matches `Fabric/template/fabric-triage.md` (same Labels behavioral rule)

- [ ] **Step 5: Commit**

```bash
git add Example/requests/workflow/default/request-template.md Example/.claude/fabric-backlog.md Example/.claude/fabric-triage.md
git commit -m "feat: sync module files and add Labels to request template"
```

---

### Task 7: Add example labels to Example entities

**Files:**
- Modify: `Example/backlog/epics/ehr-pipeline-modernization/epic.md`
- Modify: `Example/backlog/epics/ehr-pipeline-modernization/features/fhir-r4-support/feature.md`

This illustrates labels in use on real Example entities.

- [ ] **Step 1: Add labels to ehr-pipeline-modernization epic.md**

Read the file first. In the Properties section, after `Area:` (or the last property present), add:

```markdown
- Labels: service-type=data-extraction, data-domain=clinical
```

- [ ] **Step 2: Add labels to fhir-r4-support feature.md**

Read the file first. In the Properties section, after `Area:` (or the last property present), add:

```markdown
- Labels: service-type=data-extraction, data-domain=clinical, security-sensitive=true
```

- [ ] **Step 3: Verify Example entities**

Read both files and confirm Labels appears in Properties with the correct values from the Example schema.

- [ ] **Step 4: Commit**

```bash
git add Example/backlog/epics/ehr-pipeline-modernization/epic.md Example/backlog/epics/ehr-pipeline-modernization/features/fhir-r4-support/feature.md
git commit -m "feat: add example labels to ehr-pipeline-modernization entities"
```

---

## Final Consistency Check

After all tasks are complete:

- [ ] Read `Fabric/template/fabric-backlog.md` and confirm the `## Labels` section schema example uses the same nested list format as `Example/CLAUDE.md`'s `### Labels` section
- [ ] Read `Fabric/.claude/commands/rollup-backlog.md` and confirm the `Labels (rolled up)` example output format is consistent with what `fabric-backlog.md` describes in the Rollup subsection
- [ ] Read `Fabric/backlog/template-epic.md` and confirm `Labels:` placeholder text describes `key=value` format consistently with the other templates
- [ ] Confirm `Example/.claude/fabric-backlog.md` and `Fabric/template/fabric-backlog.md` are identical
- [ ] Confirm `Example/.claude/fabric-triage.md` and `Fabric/template/fabric-triage.md` are identical
